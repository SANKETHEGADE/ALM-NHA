version: '3.8'
services:
  ml-service:
    build: ./ml-service
    ports:
      - "8000:8000"
  backend:
    build: ./backend
    ports:
      - "4000:4000"
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
